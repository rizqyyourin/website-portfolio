packages:
  - '.'

# pnpm 11: dependency build scripts must be explicitly allowed via allowBuilds
# (the older onlyBuiltDependencies list was removed in v11)
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
